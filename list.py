# set defined as {} ,set([]) empty set used for set constructor
#s1 U s2 union
#s1 n s2 intersect
# difference in remove common element it effect on just one set which are first
#update is used for remove common element
#set types= union interset interset update difference difference update symentric subset superset disjoint
# set methods= add, update, copy, clear